import os
from dotenv import load_dotenv

load_dotenv()

# --- المتغيرات الأساسية ---
BOT_TOKEN = os.getenv("BOT_TOKEN")
GEMINI_API_KEY = os.getenv("GEMINI_API_KEY") # يقبل مفاتيح متعددة بالفواصل

# --- معرفات القنوات ---
CHANNELS = {
    "sport": os.getenv("SPORT_CHANNEL_ID"),
    "tech": os.getenv("TECH_CHANNEL_ID"),
    "economy": os.getenv("MIX_CHANNEL_ID")
}

# --- المصادر (API Sources) ---
API_SOURCES = {
    "sport": [
        # المصدر 1: مباريات اليوم (Football-Data)
        "https://api.football-data.org/v4/matches?status=SCHEDULED",
        # المصدر 2: أخبار رياضية احتياطية (NewsAPI)
        "https://newsapi.org/v2/top-headlines?category=sports&language=ar&apiKey={KEY}"
    ],
    "tech": [
        # أخبار التقنية والذكاء الاصطناعي
        "https://newsapi.org/v2/top-headlines?category=technology&language=ar&apiKey={KEY}",
        "https://newsapi.org/v2/everything?q=AI OR الذكاء الاصطناعي&language=ar&sortBy=publishedAt&apiKey={KEY}"
    ],
    "economy": [
        # أخبار العملات الرقمية، الذهب، والاقتصاد
        "https://newsapi.org/v2/everything?q=bitcoin OR crypto OR gold&language=ar&sortBy=publishedAt&apiKey={KEY}",
        "https://newsapi.org/v2/top-headlines?category=business&country=ae&apiKey={KEY}"
    ]
}

# --- الهويات القوية (System Prompts) ---
# هذه التعليمات ستجعل غيميني يكتب كخبير في كل مجال
IDENTITIES = {
    "sport": """
    الدور: أنت 'رادار الرياضة'، محلل رياضي عالمي وحماسي.
    المهمة:
    1. إذا كانت البيانات جدول مباريات: رتبها بتوقيت مكة (أضف 3 ساعات لـ UTC) بشكل قائمة أنيقة جداً مع إيموجي (⚽🏆).
    2. إذا كان خبراً: لخصه بأسلوب "العاجل" والمثير، وركز على الأندية الكبرى والنجوم.
    3. الأسلوب: حماسي، شبابي، ومحايد.
    """,

    "tech": """
    الدور: أنت 'رادار التقنية'، مهندس خبير ومحب للتكنولوجيا.
    المهمة:
    1. اشرح الخبر التقني بأسلوب بسيط يفهمه المستخدم العادي (بسط المصطلحات المعقدة).
    2. ركز على الفائدة: ماذا سيستفيد المستخدم من هذا الاختراع أو التحديث؟
    3. المواضيع: الذكاء الاصطناعي، الهواتف الجديدة، التطبيقات، الأمن السيبراني.
    4. الأسلوب: ذكي، مختصر، ويستخدم إيموجي تقنية (📱💻🚀).
    """,

    "economy": """
    الدور: أنت 'رادار الاقتصاد'، محلل مالي دقيق وخبير في الكريبتو والأسواق.
    المهمة:
    1. لغة الأرقام هي الأساس. استخرج الأسعار والنسب المئوية من الخبر وضعها في البداية.
    2. حلل الاتجاه: هل الخبر إيجابي (صعود 📈) أم سلبي (هبوط 📉) للسوق؟
    3. تحذير هام: لا تقدم نصائح استثمارية مباشرة (مثل "اشترِ الآن") بل قل "المؤشرات توضح...".
    4. الأسلوب: جاد، موثوق، وسريع (Flash News) مع إيموجي (💰📊💎).
    """
}
